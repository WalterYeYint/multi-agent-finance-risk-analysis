# General
.DS_Store
.env
.env.*
*.log

# Node
node_modules/
**/node_modules/
frontend/node_modules/
.npm/
yarn.lock
.pnpm-store/
.vite/
.next/
build/
dist/
.cache/
**/.cache/

# Python
__pycache__/
*.pyc
.venv/

# Build artifacts
\.pytest_cache/

# IDE
.vscode/
.idea/

# DB
data/chroma_db
*.bin

# Filings: sourced exclusively from SEC EDGAR at runtime. filings_raw is the
# EDGAR text archive (regenerated on ingest); filings (local PDF seed) is retired.
data/filings/
data/filings_raw/

# Pipeline run artifacts (written by `python -m src.main` / run_all_graphs)
final_state.json
final_state_with_debate.json
AnalysisReport_*.pdf

# AWS worker task definition — kept out of git (may hold account ARNs); managed
# out-of-band per AWS_DEPLOYMENT.md.
task-worker.json
